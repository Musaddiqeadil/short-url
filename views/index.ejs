<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL-Shrinker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="style.css">

    <style>

        /* General Styles */
        body {
            font-family: 'Roboto', sans-serif; /* Modern, clean font */
            background: linear-gradient(to bottom right, #e0f2f7, #bbdefb); /* Gradient background */
            color: #212529; /* Dark gray text */
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            margin: 0;
        }

        .container {
            background-color: rgba(255, 255, 255, 0.9); /* Semi-transparent white background */
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15); /* Modern shadow */
            width: 90%; /* Responsive width */
            max-width: 800px;
            margin: 30px auto; /* Centered container */
        }

        h1 {
            color: #1976d2; /* Primary blue color */
            text-align: center;
            margin-bottom: 25px;
        }

        /* Form Styles */
        form {
            display: flex;
            margin-bottom: 25px;
        }

        .form-control {
            flex-grow: 1;
            padding: 12px;
            border: 1px solid #bdbdbd; /* Light gray border */
            border-radius: 6px;
            margin-right: 15px;
            transition: border-color 0.3s ease; /* Smooth border transition */
        }

        .form-control:focus {
            border-color: #1976d2; /* Blue border on focus */
            outline: none;
            box-shadow: 0 0 5px rgba(25, 118, 210, 0.2); /* Subtle blue shadow on focus */
        }

        button {
            background-color: #1976d2; /* Primary blue button */
            color: #fff;
            padding: 12px 20px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #1565c0; /* Darker blue on hover */
        }

        /* Table Styles */
        .table-container {
            overflow-x: auto; /* Horizontal scroll if needed */
            border-radius: 6px; /* Rounded corners for the container */
            border: 1px solid #e0e0e0;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            table-layout: fixed;
            min-width: 600px; /* Minimum width to prevent collapsing on small screens */
        }

        th, td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #e0e0e0;
            word-break: break-all;
            position: relative;
        }

        th {
            background-color: #f5f5f5; /* Very light gray header */
            font-weight: 500;
            color: #424242; /* Darker gray header text */
        }

        tbody tr:hover {
            background-color: #fafafa; /* Very light gray on row hover */
        }

        a {
            color: #1976d2;
            text-decoration: none;
            display: block;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
            transition: color 0.3s; /* Smooth color transition */
        }

        a:hover {
            text-decoration: underline;
            color: #1565c0; /* Darker blue on hover */
            white-space: pre-wrap;
            overflow: visible;
            text-overflow: clip;
        }

        /* Tooltip Styles */
       

        /* Responsive Styles (Media Queries) */
        @media (max-width: 768px) {
            .container {
                padding: 20px;
            }

            h1 {
                font-size: 1.8rem;
            }

            form {
                flex-direction: column; /* Stack form elements vertically */
            }

            .form-control {
                margin-right: 0;
                margin-bottom: 15px;
            }

            button {
                width: 100%; /* Full-width button */
            }

            th, td {
                padding: 12px;
                font-size: 0.9rem; /* Smaller font on smaller screens */
            }
        }

        @media (max-width: 500px) {
            th, td {
                font-size: 0.8rem; /* Even smaller font on very small screens */
            }
        }
        

    </style>
    
</head>
<body>
    <div class="container">
        <h1>URL Shrinker</h1>
        <form action="/shortUrls" method="post">
            <input required type="url" name="fullUrl" id="fullUrl" class="form-control mr-2" placeholder="Paste your long URL here">
            <button type="submit">Shrink</button>
        </form>

        <table>
            <thead>
                <tr>
                    <th>Long URL</th>
                    <th>Short URL</th>
                    <th>Clicks</th>
                </tr>
            </thead>
            <tbody>
                <% shortUrls.forEach(shortUrl => { %>
                    <tr>
                        <td><a href="<%= shortUrl.full %>" target="_blank"><%= shortUrl.full %></a></td>
                        <td><a href="/<%= shortUrl.short %>" target="_blank">http://localhost:5000/<%= shortUrl.short %></a></td>
                        <td><%= shortUrl.clicks %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>